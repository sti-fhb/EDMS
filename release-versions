# EDMS 部署版本紀錄
#
# 每次要出交付包前，在檔尾新增一行並 commit —— **那個 commit 的 hash 就是
# /sti-build-release-images 預設要打包的版本**。
#
# 機制：指令以 `git log -1 --format=%H -- release-versions` 取得本檔最後一次
#       提交的 hash，據此觸發 build-release-images.yml。因此「宣告一次部署」
#       與「該次部署對應的程式碼狀態」由同一個 commit 綁定，不會對不上。
#
# ⚠️ 本檔的內容本身不影響建置 —— 有意義的是**提交這件事**。日期與說明是給人看的。
# ⚠️ 只在確定要出版時才 commit 本檔。順手改一行會讓預設打包版本跟著移動。
#
# 格式：<部署日期 YYYY-MM-DD>  <說明>
# ------------------------------------------------------------
