"""ET06 學員端測驗作答（US6 / #279）。

與教師端的 `app/et/quiz/` **刻意分開**：那裡的授權問的是「你是不是這門課的擁有者」，
本模組問的是「你在不在籍」——兩者相反。混在同一個 router 遲早有人把 dependency 掛錯
層，而那種錯誤的表現是「學員能改題目」或「學員不能作答」，都不會在寫測試時自然浮現。
比照 #255 把 `learning/`（學員端）與 `material/`（教師端）分開的同一個理由。
"""
