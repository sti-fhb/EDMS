"""ET 線下考核核可（US16 / #352）。

**與 `tracking` 分開的理由**：`tracking` 是 ET03 的唯讀聚合查詢（三區塊的清單、測驗
總覽、問卷結果），核可則是寫入 + 寄信 + 稽核。混在一起會讓一支查詢模組變成什麼都做，
而那支模組的每個方法都以「取資料再組裝」為前提、沒有任何交易語意的考量。

依賴方向為單向：`tracking` 呼叫本模組的查詢把核可狀態併進學員清單，本模組不反向依賴
`tracking`。
"""
