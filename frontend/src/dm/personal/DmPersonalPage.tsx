import { StubPage } from "../../components/StubPage"

/** 個人專區（US9 / #8）：我的文件動態 + 草稿匣 + 撤回送審（編輯者 / 審核者）。骨架佔位。 */
export function DmPersonalPage() {
  return (
    <StubPage
      title="個人專區"
      note="US9 個人專區（#8）：我的文件動態、草稿匣、撤回送審三分頁（編輯者 / 審核者）。與個人資料維護（DP 後台）為不同入口。骨架佔位，功能待對應 issue 實作。"
    />
  )
}